/* ============================================================
   Site Punch List — configuration
   ------------------------------------------------------------
   You normally DON'T need to edit this file — you can connect to
   the cloud from inside the app (Sign-in screen → "Connect cloud").
   Those in-app credentials are saved in this browser and override
   what's here.

   SUPER_ADMIN_EMAIL: whoever signs in with this email is the app's
   Super Admin (sees every project, approves/rejects any user).
   ============================================================ */
window.PUNCHLIST_CONFIG = {
  SUPABASE_URL: "https://glyxanezsslpcoksjzwx.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_W7SlgGxoDuyoMNt-0e2reQ_W-igYDdt",
  SUPER_ADMIN_EMAIL: "drsamimoha2018@gmail.com"
};
